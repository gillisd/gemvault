FROM docker.io/library/ruby:4.0.1-slim

COPY . /tmp/build/

RUN cd /tmp/build \
    && bundle exec rake build \
    && gem install --no-document pkg/*.gem shim/pkg/*.gem \
    && gem install --no-document rspec \
    && rm -rf /tmp/build
